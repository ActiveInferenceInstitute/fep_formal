# References {#sec:bibliography}

Every entry below is cited in the text. The database is `manuscript/references.bib`; the combined build resolves each inline `[@key]` with natbib and BibTeX under the `plainnat` style, and the list is typeset here rather than after the appendices.

```{=latex}
\bibliography{references}
```
