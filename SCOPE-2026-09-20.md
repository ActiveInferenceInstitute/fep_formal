# SCOPE-2026-09-20 — fep_lean improvement spec

Produced 2026-09-20 by a 7-lane read-only scoping swarm (Ledger / CI / SRC / DOCS /
TESTS / ARTIFACTS / LEAN) over `main` @ `1c3c627` (clean). Findings are real at
HEAD; two lane premises were corrected during integration (DOCS-1's doc half and
CI-lane-1's sibling-dependency premise). Spec is spec + same-day execution wave.

## Ledger truth-pass (5 open rows — verdicts, no closures)

| Row | Verdict |
|---|---|
| FEP-FULL-155 | Open, correctly gated (external credential). Probe signature `validate_report_receipt(..., require_complete=True, project_root=...)` exists (`reporter.py:304-312`). |
| FEP-RELEASE-NEXT | Open; row text stale. v1.2.0 shipped (`7886e3f`); native receipt already records v4.34.0/owner_manifest 18/155 topics/0 sorry (`output/native-verification.json:8-14`). Residual = bundle byte-identity + `--check` + DOI cross-reference. LED-1 fixes the row text. |
| FEP-SCAFFOLD-PORTABILITY | Open, optional remainder only. Guard + pinned-digest test live (`gnn_continuous_artifact_proof.py:244-247`, `tests/test_gnn_continuous_artifact_proof.py:330-366`). |
| FEP-H27-RESEAL | Open, H3.0-gated adjudication only. `1c3c627` re-recorded the chain post-`69400a1`; whether the row's `f4dbbdba` digest still matches is UNVERIFIED — LED-2 runs the row's own probe and records the verdict. |
| FEP-H3-SCIENCE | Open, correctly gated. H3.G0 done, H3.0 not frozen, independent claim review next. LED-3 flags item 1's 2026-09-11 premise as predating the rebind chain (carried into the adjudication, not edited now). |

SCOPE-2026-09-15 dispositions: N-1 RESOLVED (released v1.2.0 section), N-2
RESOLVED (`0f5394b`), N-3 RESOLVED via option (a) + named-source attribution.

## Improvement items

### Medium

| ID | Category | Motivation (evidence) | Next step | Probe | Effort |
|---|---|---|---|---|---|
| CI-2-1 | CI gate gap | The two bridge `--check` gates AGENTS.md requires are missing from CI — and are sibling-independent: `specs/gnn-bridge-q5-artifact-proof/generate_probe.py --check` regenerates in memory from committed fixtures; `specs/geo-infer-notation-bridge/check_geo_notation_bridge.py --check` validates local YAML only (ci.yml:28-40) | Add both steps to the python job | ci.yml step list contains both; CI green | S |
| CI-2-2 | CI caching | lean job recompiles from scratch every run; 180m timeout has ~27% headroom over the ~2.2h local build (ci.yml:216-217, 252) | Cache `lean/.lake/build` keyed on toolchain+lakefile+manifest+fep_all.lean | workflow yaml; actionlint/`gh workflow view` sanity | S |
| CI-2-4 | CI triggers | `pull_request` (ci.yml:4-8) has no paths filter — doc-only PRs pay ~3h lean+render | `paths-ignore` for `docs/**`, `*.md`, receipts-adjacent docs | yaml; doc-only PR diff shows skipped lean job | S |
| SRC-1 | Duplicated logic | `manuscript.py:766-799` reimplements fsutil atomic writes; fsutil import at :23 dead | Fold into `output/fsutil.py` + `manuscript.py` (SC-14 owner) | `rg 'def (_stage_bytes|_atomic_write'` → 0 in manuscript.py | M |
| SRC-3 | Silent fallback | `pipeline/core.py:163-164` swallows settings.yaml load errors, silently redirects output root | Fail closed when the file exists but is unreadable | corrupt-settings tmp root raises | S |
| SRC-4 | Duplicated logic | `gauss/runner.py:265-335` vs `337-410` duplicate ~55 lines per-topic logic | One `_run_logged_topic` helper in runner.py | `rg -c 'Unhandled runner exception'` → 1 | M |
| SRC-5 | Missing fail-closed | `release_bundle.py:2227-2231` `except ValueError: continue` silently skips non-integer coverage lines from the ownership check | Append a receipt error instead | malformed line record yields error | S |
| SRC-6 | Silent fallback | `llm/hermes.py:286-287` broad except falls back to default model/base_url on malformed settings.yaml | Narrow to (OSError, UnicodeError, yaml.YAMLError); fail closed on malformed existing file | corrupt settings → raises; absent unchanged | S |
| TESTS-1 | Runner adoption | ~20 test files still raw `subprocess.run` lean compiles with no process-group handling (`tests/_support/lean_runner.py:3-7` documents the orphan hazard); `_lake_executable` duplicated 27+ files | Extend `run_lean_probe` variants; adopt in the ~20 files; dedupe `_lake_executable` via `_support` | no `'"env", "lean"'` compile sites in the named files | M |
| TESTS-3 | Duplicated normalizer | `_without_lean_comments` copy-pasted verbatim in 21 test files; canonical `fep_lean.lean_source.lean_code_without_comments` already public and imported elsewhere | Delete locals; import canonical (offset-preserving; verify per-file no shifted assertions) | `rg -l "def _without_lean_comments" tests` → 0 | M |
| ARTIFACTS-1 | Receipt classification | N-3(b/c) residue: 09z digested as a peer of authored chapters; docstring :443-446 states a false boundary; missing 09z hashes `b""` (:477-478) vs sibling fail-closed (`render_fonts.py:47-62`); moved-09z remediation text says "re-run the render" (:598-609) | Classify `VERBATIM_SOURCES` names in `_moved_sources`/`receipt_defects`; fix docstring; fail closed on missing generated source; keep RECEIPT_VERSION (no committed-receipt edit) | regression: receipt with only-09z moved names `fep-lean catalogue` | M |
| ARTIFACTS-5 | Config doc drift | `config/README.md:9` claims nonexistent `orchestration.*`; `docs/configuration.md:10-11` document unread `gauss.default_model`/`verify_lean`; dead keys `gauss.source/log_level`, `output.report_dir` (`reporter.py:1530` hardcodes) | Correct README/configuration.md; trim-or-mark dead keys in settings.yaml | greps return corrected text only | M |
| DOCS-5 | Duplicated scaffolding | Six near-identical `--check`/write shells (`cli.py:129-171`, `build_formalism_atlas.py:19-28`, `build_formal_kernel_dashboard.py:19-28`, `build_formalism_coverage.py:19-28`, `_maint_build_formal_modules.py:22-31`, `_maint_build_topics_catalogue.py:21-31`); lowercase `stale:` drift in topics-catalogue; `cli.py:197` broad except (SRC-8) | Shared `check_or_write` helper in existing rostered owner `fep_lean/catalogue/generation.py`; thin all callers; unify wording; narrow cli except to (OSError, ValueError, TypeError) | all six emit identical wording; exit 1 on drift | M |
| LEAN-5 | Spec archive (custody-gated) | `specs/horizon-2-smooth-stochastic` delivered (H2.7 accepted) but reads active; move is invalidated by path-keyed digests (`readiness/acceptance.json:44-49`) | **Carried, not in wave**: ride the next coordinated custody refresh (FEP-RELEASE-NEXT cadence) | after refresh: dir under done/ + validators green | M |

### Minor

| ID | Category | Motivation | Next step | Probe | Effort |
|---|---|---|---|---|---|
| CI-2-3 | CI fail-open | lean Mathlib cache fetch (ci.yml:242-245) echoes and continues on miss → silent 2h source build near timeout | Mirror render job's fail-closed copy (ci.yml:131-134) | yaml | S |
| CI-2-5 | CI drift | `lean-latest.yml:31` bare `python3` vs ci.yml's `uv run` (stdlib-only today, `pin_audit.py:57-67`) | Switch to `uv run` | yaml | S |
| SRC-2 | Dead code | Deprecated `_sha256`/`_atomic_text`/`_atomic_bytes` shims in browser_capture:305, evidence:41, release_bundle:286+318, rendering:268-275, reporter:299-301; reporter:1700 inline hashlib; `gnn_artifact_proof.py:155` second sha256_file with divergent missing-file contract | Replace callsites with fsutil imports; align or rename the divergent duplicate | `rg 'Deprecated alias'` → 0 | S |
| SRC-7 | Duplication | `cli.py` `_atlas`/`_dashboard` byte-twins + 4x relative_to fallback (also manuscript.py:677-680, rendering.py:182-185) | Parameterized helpers in cli.py (fsutil `_display` for the fallback) | atlas/dashboard --check exits 0/1 correctly | S |
| SRC-8 | Error types | `cli.py:197` `except Exception` converts bugs into clean JSON filter errors | Narrow per file convention | `rg 'except Exception' cli.py` → 0 | S |
| TESTS-2 | Marker gap | `tests/test_bridge_verify_document.py:56` compiles Lean with no `serial_lean` marker — escapes both the -m filter and xdist pinning | Add `pytestmark = pytest.mark.serial_lean` | collect-only shows marker | S |
| TESTS-4 | Source-text assertion | `test_formalism_presentation.py:70-77` asserts raw src text (comment mention fails it; alias call passes it) | Behavioral monkeypatch probe in same file | rewritten test red under injected forbidden call | S |
| TESTS-5 | Private coupling | `test_coverage_gaps.py` `_has_sorry`/`_get_timeout`/`client._conn` raw SQL/`hasattr` guard | Public-seam probes; small read_artifact accessor folded into `gauss/client.py` | `rg -n "_conn.execute|_has_sorry|_get_timeout" tests/test_coverage_gaps.py` → 0 | M |
| TESTS-6 | CI skip | `test_render_fonts.py:71-75` skips the font audit on every CI runner | Stub-aware expectation (mirror :66-68) | CI=true runs (not skips) the audit | S |
| ARTIFACTS-2 | Degraded check | `check_render_log.py:116-124` silently skips placeholder lines when `manuscript_vars.yaml` absent while printing unconditional OK | Print a distinct degraded-mode WARN | fixture without vars asserts WARN | S |
| ARTIFACTS-3 | Missing gate | `render_publication.py:150` never passes `--require-release-stamp`; docstring names it as the archiving hard gate | Add `--require-release-stamp` pass-through, default off | composition with flag on past-tag checkout asserts nonzero | S |
| ARTIFACTS-4 | Log asymmetry | `render_log.py:406-408` overflow scan hardcodes `_combined_manuscript.log` and returns `()` silently when absent while stdout log exists | Scan whichever expected log exists, else defect | fixture with only stdout log yields defect | S |
| ARTIFACTS-6 | Hand-typed counts | "five areas" ×3 chapters; ten/five/fifteen family counts ×5 places vs existing `{{total_areas}}` + `manuscript_vars` families projection | Tokenize areas; add `expansion_families` derived values in `build_manuscript_vars`; replace literals | `render_manuscript.py --check` green; grep hand-typed → 0 | S |
| ARTIFACTS-7 | Lock fragility | `render_publication.py:250-251` `finally: lock.rmdir()` can raise OSError replacing real exit code; dead lock busy-waits 1800s | Guard rmdir + holder PID in lock | pre-created non-empty lock returns result + warning | S |
| LEAN-1 | Build wrapper | `lean/build.sh:16` computes SCRIPT_DIR but runs lake in caller's cwd — root invocation fails (`.lake/build.log:1-9`) | `cd "$SCRIPT_DIR"` after setup | root-cwd invocation exits 0 after | S |
| LEAN-2 | Docs drift | `lean/README.md:3`, `lean/AGENTS.md:3` say v1.1.0; `lean/AGENTS.md:57` v4.33.1 toolchain path; `lean/README.md:64` stale ELAN_HOME literal (truth: `_toolchain.py:109-112`) | Update the four files' stale refs (leave `lean_verifier.py:18` docstring to next pin cycle) | grep v1.1.0 / v4.33.1 / /tmp/fep_lean_elan in lean/README+AGENTS → 0 | S |
| LEAN-3 | Spec status | P1/Q1/Q2 README status lines still "active" though accepted (`7669610`, `9a5dcdf`; design doc :3) | Replace with accepted wording in 3 lines | `rg 'Status: \*\*active' specs --include=README.md` → 0 | S |
| LEAN-4 | Spec archive | `specs/fep-lean-review-2026-09-05` is a point-in-time REPORT pinned to f335724; findings landed; no path-keyed receipts bind it | `git mv` under `specs/done/` + closed-status header | dir moved; no inbound links break | S |
| LEAN-6 | Spec status | `geo-infer-notation-bridge/README.md:3` frozen at "opened 2026-09-08" though checker landed (`1bab5f5`, `e7afc36b`, `11d582b`, `267020f`) | Status line records core-subset landed + open growth protocol | checker --check still exits 0 | S |
| LEAN-7 | Phantom path | `specs/h3-reference-study/README.md:13-14` documents `output/h3-pre-outcome-metadata.json` (nonexistent); real instance is `specs/h3-case-study/pre-outcome-metadata.json` | Point example at the recorded instance | grep ghost path → 0; replacement resolves | S |
| DOCS-1' | Audit regex | `docs/pin_audit.py:91` `_RE_MATHLIB` cannot match backtick-quoted `` Mathlib `vX` `` forms (audit green while prose is stale). Note: `docs/lean4.md` is ALREADY correct at HEAD — the lane's doc half is withdrawn | Harden regex for optional backtick | hardened regex flags a planted backtick form; clean tree green | S |
| DOCS-2 | Doc drift | `docs/coverage-branch.md:3-4` + `pyproject.toml:67` claim ProcessPoolExecutor in figures.py; repo-wide grep zero | Correct both; verify coverage behavior before touching concurrency key | grep ProcessPoolExecutor src scripts → 0; doc clean | S |
| DOCS-3 | Release line | `HANDOFF.md:6` says v1.1.0; `HANDOFF.md:203-204`, `ISA.md:125-127` version-stamped archive names (v1.2.0 shipped) | Update release line; de-version archive names | `grep -n '1\.1\.0' HANDOFF.md ISA.md` → 0 | S |
| DOCS-4 | Quickref | `docs/quickref.md:18` bridge line omits required `--gnn-root` (bridge/cli.py:31-36) | Add the flag to the line | corrected line composes | S |
| LED-1 | Ledger row | FEP-RELEASE-NEXT row premise false at HEAD (receipt at v4.34.0; v1.2.0 shipped) | Coordinator-owned TODO row rewrite (docs-only) | `grep -c "4\.33\.1" TODO.md` → 0 | S |
| LED-2 | Ledger row | FEP-H27-RESEAL row digest `f4dbbdba` freshness after `1c3c627` unverified | Coordinator runs the row's own `validate_terminal_acceptance` probe and records the live verdict in the row | probe exits 0; row cites live digest | S |
| LED-3 | Gated record | H3 record item 1's drift list predates `69400a1`/`45df4be`/`1c3c627` | **Flag only** — carried into the H3.0 adjudication; the record is not edited now | at adjudication: reconcile or reword item 1 | S |

### Test-measurement item (wave-end, not a file edit)

| ID | Category | Next step |
|---|---|---|
| TESTS-7 | Coverage fidelity | One serial-inclusive coverage run (no `-m` filter) after the wave lands; record per-module delta table in `docs/coverage-branch.md`. Requires the Lean toolchain; runs after the gate battery. |

## Execution order (this wave)

Wave workers (disjoint file ownership, parallel): CI (ci.yml+lean-latest.yml) |
render_log (render_log.py+check_render_log.py+test_render_log.py) |
render_publication (render_publication.py+test_render_publication.py) | fsutil
sweep (fsutil.py, browser_capture, evidence, release_bundle, rendering, reporter)
| manuscript (manuscript.py+chapters: SRC-1+ARTIFACTS-6) | fail-closed/dedup
(core.py, runner.py, hermes.py) | check_or_write (cli.py,
catalogue/generation.py, 7 scripts) | tests-lean (lean_runner adoption +
normalizer dedup + TESTS-2) | docs/config (DOCS-1',2,3,4 + ARTIFACTS-5) |
lean/specs (LEAN-1..4,6,7) | tests-minor (TESTS-4,5,6).

Then: coordinator folds TODO.md (LED-1), runs the Python gate battery, LED-2
probe, fix-ups, ledger fold (CHANGELOG), commits, push.

Carried (not this wave): LEAN-5 (custody-gated move), TESTS-7 (wave-end
measurement), LED-3 (adjudication flag), the 5 open TODO rows.
