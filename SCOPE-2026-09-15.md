# SCOPE-2026-09-15 — fep_lean improvement spec

Review-and-scope phase output. **Nothing below is implemented.** Produced
2026-09-15 by a lightweight review pass over the tree at HEAD `24f570a`
(`main`, clean at spawn; PR #19 toolchain program + pin cycle #7 + fixture
normalization all landed, CI green).

## Method (probes run)

All probes were lightweight (no full test suite, no `uv sync`, no docker, no
network fetches beyond what `pin_audit --check-latest` itself performs):

| Probe | Result |
|---|---|
| `git log --oneline -12`, `git status` | clean at `24f570a`; recent history: `24f570a` fixture normalization ← `430b725` pin cycle #7 ← `63aac26` PR #19 merge ← `88ffd01` v4.34.0 bump + coordinated custody refresh |
| `uv run python docs/pin_audit.py --check-latest` | **OK: 222 file(s) scanned — no drift.** Canonical pair is `v4.34.0` (toolchain + Mathlib, revision `5ed29652…`); newest stable matches |
| `uv run fep-lean status` | composes (exit 0): catalogue products **current**; bridge source pin **current** (fep_lean `63aac26`, GNN `40eda95f`); render receipt **stale locally** (see N-3 — caused by the gitignored generated `manuscript/09z_unified_formalism_catalogue.md`, `.gitignore:58`, not by committed bytes); native receipt **not_claim_ready** — the receipt present in local `output/` still records `lean_toolchain: v4.33.1`, `owner_manifest_version: 17` (`output/` is gitignored; CI regenerates and validates it fresh per `ci.yml:257-272`, so this is local residue, not a repo defect) |
| Terminal-receipt custody diff (python sha256 over `current_sources` vs live tree) | exactly **one** drifted file: `src/fep_lean/verification/horizon_acceptance.py` (the validator's own source binding); the other previously-drifted files (`latex.py`, `registry.py`, `declarations.py`, `lean_source.py`, `tests/conftest.py` R0 map) match the sealed receipt |
| R0 successor custody diff (`specs/horizon-2-smooth-stochastic/readiness/repairs/07-gaussian-vfe-natural-gradient-custody.json` `source_sha256` vs live tree) | **no drift** — the manifest binding the 09-09 row feared unbound is intact |
| `git log f335724..HEAD -- src/fep_lean/verification/horizon_acceptance.py tests/conftest.py src/fep_lean/formal/manifest.py` | validator-source edits at `5fc0246` (SC-6 strict-JSON consolidation), `49c99c7`, `740b595` (conftest), `88ffd01`; no post-seal `manifest.py` change |
| Existence check: `specs/horizon-2-smooth-stochastic/verify_native.py` | **does not exist** and never did (empty `git log --follow` for the path); the row's documented probe cell was pointing at a phantom script. The real validator is `fep_lean.verification.horizon_acceptance.validate_terminal_acceptance` (`horizon_acceptance.py:513`), which currently fails closed with "current validator/diagnostic source mismatch" (`horizon_acceptance.py:574`) |
| CHANGELOG sweep (`grep 4.34 / toolchain / #19 / pin cycle #7`) | **zero hits** — the 2026-09-15 toolchain program is unrecorded (N-1) |
| Version-string sweep (`grep '4\.33\.' tests/ scripts/ …`; `grep '4\.34\.0'`) | the only `4.33.1` hits are synthetic fixtures in `tests/test_pin_audit_latest.py` (fake "latest" tags — correct by construction); `tests/test_reporter.py` / `tests/test_native_evidence.py` no longer hardcode a version (bump-proof fixtures at `1534610`/`24f570a`); prose references to `4.34.0` are current (`lean/README.md:5`) except `ISA.md:36` (N-2) |
| `.github/workflows/ci.yml` lean job read (`:215-307`) | compiles `FepSketches` warning-free (`:246-256`), regenerates + validates the native receipt claim-ready (`:257-272`), audits formalisms zero-error (`:284-301`), `timeout-minutes: 180` (`:217`) |

## Cleared-Item-Evidence (TODO.md truth pass)

**Cleared: 0.** All 6 open rows re-verified against `24f570a`. Per-row record:

| TODO row | Verdict | Evidence (2026-09-15) |
|---|---|---|
| `FEP-FULL-155` | Open (unchanged) | Externally gated: no provider run, receipt, or credential wave appears anywhere in the recent history; historical 50-topic/one-topic reports correctly remain historical (HANDOFF.md:24-39). Nothing changed since 2026-09-09. |
| `FEP-LEAN-UPGRADE` | **Engineering complete; row kept open on a docs-only residual** | Every technical probe condition is met at `24f570a`: pins are `v4.34.0` (`lean/lean-toolchain:1`, `lean/lakefile.lean:16`, manifest revision `5ed29652…`); the CI lean lane compiles warning-free, regenerates the native receipt, and validates `native_claim_ready` + zero formalism-audit errors against the live roster (`ci.yml:246-301`, green at `24f570a`); `docs/pin_audit.py --check-latest` passes locally (222 files, no drift); the newest-stable gate now reports the pinned pair current. The one unmet closure-rule condition is changelog recording: CHANGELOG.md's newest entry is still "Docs pinning sweep and bridge re-pins #13/#14 (2026-09-14/15)" (`CHANGELOG.md:3`) — no entry exists for `88ffd01` / PR #19 / pin cycle #7 / the fixture normalization. Row rewritten in place to exactly this residual. |
| `FEP-RELEASE-NEXT` | Open (re-anchored) | Version is still `1.1.0` (`pyproject.toml:7`, `CITATION.cff:21`) with CHANGELOG head `## Unreleased`; version number and DOI remain open decisions exactly as the row says. Updated wording: the release must now bind the `v4.34.0` tree, so a fresh claim-ready native receipt at the new pin (the committed-era `output/native-verification.json` records `v4.33.1`, and a full native sweep is a ~79-minute compile: `duration_s: 4747.883`) plus the H2.7 re-seal precede the byte-identical bundle work. |
| `FEP-SCAFFOLD-PORTABILITY` | Open (unchanged) | The fail-closed interpreter contract recorded in the row landed at `fa931c8` (in-module `scaffold_digest` guard, pinned digest unchanged — commit message and row agree); the optional multi-interpreter remainder stands as written. |
| `FEP-H27-RESEAL` | Open — **stale-evidence claim corrected; probe cell fixed** | The row's 2026-09-12 five-file drift list is outdated: four of five files plus the `conftest.py` R0 map now match the sealed receipt, and the `07-gaussian-vfe-natural-gradient` R0 successor custody shows no drift. The surviving residual is precise: `horizon_acceptance.py` itself drifted post-seal (`5fc0246`/`49c99c7`/`88ffd01`), so the receipt cannot revalidate until a coordinated re-seal refreshes the validator-source binding. Additionally, the row's acceptance probe cited `specs/horizon-2-smooth-stochastic/verify_native.py --check`, a script that does not exist (and never did); the probe cell now names the real validator invocation. The H3.0 adjudication gate (Horizon 3 record open item 1) is untouched and still gates the re-seal's downstream. |
| `FEP-H3-SCIENCE` | Open (unchanged) | Gated preregistered case study; eligibility validator implemented (`validate_continuous_eligibility`, `horizon_acceptance.py:694`), no G0 study metadata frozen. Consistent with `FEP-H27-RESEAL` remaining the active H2-side custody item. |

## Carried-Open

- `FEP-FULL-155` — externally gated (credential + spend boundary).
- `FEP-H3-SCIENCE` — gated on the H3.0 adjudication and preregistration.
- `FEP-SCAFFOLD-PORTABILITY` — optional remainder, already precisely scoped in the row.
- The release program (`FEP-RELEASE-NEXT` + the native-evidence refresh at the
  new pin) — see the major item below; it is the only newly-ordered work.

## Improvements

Fields per item: ID / Severity / Category / Motivation / Scope / Acceptance
probe / Effort.

### N-1

| Field | Content |
|---|---|
| ID | N-1 |
| Severity | minor |
| Category | docs consistency / closure integrity |
| Motivation | CHANGELOG.md has **no entry for the 2026-09-15 toolchain program**: `88ffd01` (v4.34.0 bump + coordinated evidence-custody refresh), the PR #19 merge `63aac26`, pin cycle #7 `430b725`, and the bump-proof fixture normalization `24f570a` are unrecorded — the newest Unreleased entry is still the 2026-09-14/15 bridge re-pins (`CHANGELOG.md:3`; a `grep` for `4.34`/`toolchain`/`#19`/`pin cycle #7` in the Unreleased head returns nothing). The repo's own TODO closure rule makes changelog recording a condition for `FEP-LEAN-UPGRADE` to leave the backlog, so this single docs edit is the row's entire residual. |
| Scope | `CHANGELOG.md` only: add one Unreleased entry (in the style of the 2026-09-14/15 commit-by-commit entries) covering the bump, the coordinated custody refresh, pin cycles #6/#7, and the fixture normalization. Docs-only; receipt-neutral. |
| Acceptance probe | `grep -n "4\.34\.0" CHANGELOG.md` → ≥1 hit under `## Unreleased`; `uv run python docs/pin_audit.py --check-latest` still passes; `uv run python docs/md_hygiene.py --strict` clean. |
| Effort | S (≤30 min) |

### N-2

| Field | Content |
|---|---|
| ID | N-2 |
| Severity | minor |
| Category | docs accuracy |
| Motivation | `ISA.md:36` describes the scope boundary as "Exact Lean **4.34.0** and Mathlib **`v4.33.1`**" — a mismatched pair (Lean and Mathlib ship as a locked pair; `lean/lean-toolchain:1` and `lean/lakefile.lean:16` are both `v4.34.0`). The line predates the coordinated bump and now asserts an impossible toolchain state in the repository's ideal-state artifact. |
| Scope | `ISA.md:36` only: correct the Mathlib tag to `v4.34.0` (or rephrase as "the pinned Lean/Mathlib pair"). Docs-only. |
| Acceptance probe | `grep -n "4\.33\.1" ISA.md` → 0 hits; pair agrees with `lean/lean-toolchain` and `lean/lakefile.lean`. |
| Effort | S (≤30 min) |

### N-3

| Field | Content |
|---|---|
| ID | N-3 |
| Severity | medium |
| Category | DX / evidence-custody friction |
| Motivation | The render-receipt freshness chain binds a **committed** receipt (`docs/render-acceptance.json`, re-accepted at `88ffd01`) to a **gitignored, generated** manuscript source (`manuscript/09z_unified_formalism_catalogue.md`, `.gitignore:58`). Consequence: any local `uv run fep-lean catalogue` (or any wave that regenerates projections, e.g. the `98c6324` coverage regeneration) makes the shipped receipt locally stale — `fep-lean status` reports `render_receipt_freshness: stale` naming exactly that file — and clearing it requires the full publication render stack (shared-template checkout, XeLaTeX, pandoc, `rsvg-convert`, mermaid CLI, two fonts — HANDOFF.md:230-236), which CI cannot run (`ci.yml:103-114` verifies the receipt but produces none). CI is green because it regenerates `09z` deterministically; every human/agent checkout instead carries a permanently stale-looking receipt until a full local render. This burned probe time in this very pass and will mislead the next status-reading agent. |
| Scope | Owner decision, docs-first: either (a) document in HANDOFF.md/README that a stale `render_receipt_freshness` naming only `09z_*` is expected local residue and how to distinguish it from a real defect, or (b) make `receipt_defects` classify gitignored generated sources separately from committed manuscript sources, or (c) bind the receipt to the committed manuscript subset only. (b)/(c) touch `src/fep_lean/output/render_log.py` + tests → must ride a coordinated evidence refresh per the pin-ordering rule. |
| Acceptance probe | Fresh clone + `uv run fep-lean catalogue` + `uv run fep-lean status`: the render section either reads `current` or its `stale` findings explicitly attribute the gap to the generated `09z` source (option b/c), and HANDOFF.md documents it (option a). |
| Effort | M (a: ≤1 h docs; b/c: ≤3 files, ≤2 h + one custody re-pin) |

### N-4

| Field | Content |
|---|---|
| ID | N-4 |
| Severity | major |
| Category | program: coordinated evidence refresh + release at the v4.34.0 pin |
| Motivation | The `v4.34.0` program landed with CI-proven fresh evidence, but every **retained** claim-ready surface still records the `v4.33.1` world: the native receipt archetype (`owner_manifest_version: 17`, `lean_toolchain: v4.33.1`), the H2.7 terminal receipt (validator-source mismatch, `FEP-H27-RESEAL`), and the release plane (`FEP-RELEASE-NEXT`, still `1.1.0`). A full native sweep is a ~79-minute compile (`duration_s: 4747.883`; CI lean budget 180 min, `ci.yml:217`), the H2.7 re-seal is gated on the H3.0 adjudication (Horizon 3 record open item 1), and the release adds byte-identical bundle + DOI + notes. These three are one coordinated custody wave, not three independent tasks — per the repo's own pin-ordering rule (`0be4b46`), source-adjacent changes land before the pin cycle, and N-1/N-2 (docs) land inside this wave's receipt-neutral prefix. |
| Scope | Sequencing only; the constituent work is already owned by `FEP-LEAN-UPGRADE`'s residual (N-1), `FEP-H27-RESEAL`, and `FEP-RELEASE-NEXT`. Wave 1 (docs: N-1, N-2, N-3a) → one coordinated refresh wave (H2.7 re-seal after the H3.0 adjudication; native receipt regeneration at the pin) → release engineering. No new code surface proposed here. |
| Acceptance probe | `FEP-H27-RESEAL`'s (new) validator probe passes; a fresh claim-ready native receipt validates at the `v4.34.0` pin; two `build_release_bundle.py` runs byte-identical with `--check` claim-ready against the live roster (the row's own probe). |
| Effort | XL (multi-day, multi-PR; CI/compile-bound) |

---

Prioritized order: **N-1 → N-2** immediately (docs-only, unblock
`FEP-LEAN-UPGRADE` closure); **N-3** as docs-first (a) with (b)/(c) deferred
into the next coordinated wave; **N-4** as the wave container for the carried
release/reseal program.
